#include <stdio.h>
#include <stdint.h>

int a[256][256];

int main() {
  printf("Hello, Spike!\n");
  // volatile int tmp;
  //
  // for (size_t j = 0; j < 256; j++) {
  // 	  for (size_t i = 0; i < 256; i++) {
  // a[i][j] = 0;
  // 	  }
  // }
  //
  // for (size_t j = 0; j < 256; j++) {
  // 	  for (size_t i = 0; i < 256; i++) {
  // tmp = a[i][j];
  // a[i][j] = 1;
  // 	  }
  // }

  return 0;
}
